version: '3.8'
services:
  crm-app:
    build: .
    container_name: crm-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - n8n-server_default

networks:
  n8n-server_default:
    external: true
